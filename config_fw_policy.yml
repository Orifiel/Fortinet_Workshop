---
- hosts: fortigates
  gather_facts: no

  connection: httpapi
  collections:
    - fortinet.fortios
  vars:
    vdom: "root"
    ansible_httpapi_use_ssl: no
    ansible_httpapi_validate_certs: no
    ansible_httpapi_port: 80

  tasks:
    - fortios_firewall_policy:
        vdom: "{{ vdom }}"
        state: "present"
        firewall_policy:
          name: "vpn_VPN-REDHAT_local"
          policyid: "1"
          srcintf:
            - name: "port2"
          destintf:
            - name: "VPN-REDHAT"
          srcaddr:
            - name: "VPN_REDHAT_local"
          dstaddr:
            - name: "VPN_REDHAT_remote"
          action: "accept"
          schedule: "always"
          service:
            - name: "ALL"
          comments: "VPN created by Ansible"
